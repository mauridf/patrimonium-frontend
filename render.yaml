services:
  - type: web
    name: patrimonium-frontend
    runtime: static
    buildCommand: npm install && npm run build:prod
    staticPublishPath: ./dist/patrimonium-frontend/browser
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
    envVars:
      - key: NODE_VERSION
        value: 20.x